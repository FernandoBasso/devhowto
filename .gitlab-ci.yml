image: python:latest
pages:
  stage: deploy
  only:
    - main
  script:
    - pip install mkdocs-material plantuml_markdown
    - mkdocs build --site-dir public
  artifacts:
    paths:
      - public
